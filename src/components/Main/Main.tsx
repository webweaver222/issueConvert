import React from "react";

import "./Main.scss";

const Main = ({}) => (
  <div className="MainWrapper">
    <div className="main">
      <h2>Pick a repository...</h2>
      <input type="text" />
      <button>Find</button>
    </div>
  </div>
);

export default Main;
